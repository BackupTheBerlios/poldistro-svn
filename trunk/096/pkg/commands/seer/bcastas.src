use uo;
use basic;


program bcastas(who, params)
	var name;

	if (!params)
		//TODO
		return;
	endif

	params := SplitWords(params);
	
	if (params.size() <= 1)
		return;
	elseif (params.size() >= 2)
		name :=params[1];
		Broadcast(name + ": ");
		//TODO
	endif

	return;
endprogram
