use uo;
use os;

include ":spawnsystem:include/groups";
include ":spawnsystem:include/realms";
include ":spawnsystem:include/regions";
include ":spawnsystem:include/errors";

program SpawnSystem()
	if ( !GetGlobalProperty("SpawnSystem") )
		var script := Start_Script(":spawnsystem:firstrun");
		if ( script.errortext )
			SSErrorReport(script.errortext, SPAWN_ERROR_SYSLOG);
			return 0;
		endif
	endif
	
	foreach 
	
	return 1;
endprogram
